Чёткая диагностика по логу и скринам:

Все масти = clubs (♣) — лог показывает 4c Kc, Tc, Ac, 6c... всё как трефы. На скрине K♥ A♦ J♥, а читает как клубы
Стол не обнаружен — использует ручные регионы из config.json, которые от WPC пресета (неправильные координаты для Ton Poker)
Telegram показывает ⬛ — это как раз все трефы ♣ в тёмной теме выглядят как чёрные кружки
Читаю card_utils.py и config.json параллельно:


6 actions
Теперь картина полностью ясна.

Корень проблемы: ensure_config применяет пресет только когда regions пустые (строка 147: if profile and not cfg.get("regions")). Но у пользователя config.json уже заполнен — либо WPC-пресетом с прошлого раза, либо из config.example.json (мобильный портрет, координаты Ton Poker мобильный). Ton Poker desktop использует другие координаты. Поэтому регионы указывают не туда → OCR читает тёмный фон → Phase 1 не находит насыщенных пикселей → Phase 2 возвращает "c" (клубы по умолчанию) → всё трефы.

Правлю auto.py: запоминаем active_preset в config.json и при смене игры (WPC→Ton Poker) автоматически перезагружаем регионы:


2 actions
Правка готова. Вот что происходило и что теперь будет:

Диагноз:

Твой config.json имел координаты от мобильного Ton Poker (портрет, 468×847) — они попали туда с первого запуска из config.example.json. Но ты играешь в десктопную версию (ландшафт, ~1040×585). Из-за этого:

Регионы указывали на пустой тёмный фон стола
OCR смотрел не туда → видел тёмные пиксели
Функция определения масти не находила цветных пикселей → падала в "по умолчанию трефа (♣)"
Все карты = Xc
Что исправлено:

auto.py теперь отслеживает active_preset в config.json. При следующем запуске он увидит что был WPC/мобильный пресет и автоматически переключится на Ton Poker Desktop с правильными координатами. Ты увидишь:

🔄 Профиль сменился → пресет обновлён: Ton Poker

Что делать:

Скачай обновлённый auto.py с Replit
Запусти [1] Авто-режим
Убедись что получил сообщение про обновление пресета
Сыграй руку — теперь масти должны читаться правильно (K♥ A♦ J♥, а не Kc Ac Jc)
Если после этого координаты всё ещё немного смещены (зависит от точного разрешения твоего экрана) — запусти [3] Калибров